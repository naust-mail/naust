// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"naust/daemon/internal/store/ent/predicate"
	"naust/daemon/internal/store/ent/zoneserial"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// ZoneSerialQuery is the builder for querying ZoneSerial entities.
type ZoneSerialQuery struct {
	config
	ctx        *QueryContext
	order      []zoneserial.OrderOption
	inters     []Interceptor
	predicates []predicate.ZoneSerial
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ZoneSerialQuery builder.
func (zsq *ZoneSerialQuery) Where(ps ...predicate.ZoneSerial) *ZoneSerialQuery {
	zsq.predicates = append(zsq.predicates, ps...)
	return zsq
}

// Limit the number of records to be returned by this query.
func (zsq *ZoneSerialQuery) Limit(limit int) *ZoneSerialQuery {
	zsq.ctx.Limit = &limit
	return zsq
}

// Offset to start from.
func (zsq *ZoneSerialQuery) Offset(offset int) *ZoneSerialQuery {
	zsq.ctx.Offset = &offset
	return zsq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (zsq *ZoneSerialQuery) Unique(unique bool) *ZoneSerialQuery {
	zsq.ctx.Unique = &unique
	return zsq
}

// Order specifies how the records should be ordered.
func (zsq *ZoneSerialQuery) Order(o ...zoneserial.OrderOption) *ZoneSerialQuery {
	zsq.order = append(zsq.order, o...)
	return zsq
}

// First returns the first ZoneSerial entity from the query.
// Returns a *NotFoundError when no ZoneSerial was found.
func (zsq *ZoneSerialQuery) First(ctx context.Context) (*ZoneSerial, error) {
	nodes, err := zsq.Limit(1).All(setContextOp(ctx, zsq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{zoneserial.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (zsq *ZoneSerialQuery) FirstX(ctx context.Context) *ZoneSerial {
	node, err := zsq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ZoneSerial ID from the query.
// Returns a *NotFoundError when no ZoneSerial ID was found.
func (zsq *ZoneSerialQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = zsq.Limit(1).IDs(setContextOp(ctx, zsq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{zoneserial.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (zsq *ZoneSerialQuery) FirstIDX(ctx context.Context) int {
	id, err := zsq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ZoneSerial entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ZoneSerial entity is found.
// Returns a *NotFoundError when no ZoneSerial entities are found.
func (zsq *ZoneSerialQuery) Only(ctx context.Context) (*ZoneSerial, error) {
	nodes, err := zsq.Limit(2).All(setContextOp(ctx, zsq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{zoneserial.Label}
	default:
		return nil, &NotSingularError{zoneserial.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (zsq *ZoneSerialQuery) OnlyX(ctx context.Context) *ZoneSerial {
	node, err := zsq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ZoneSerial ID in the query.
// Returns a *NotSingularError when more than one ZoneSerial ID is found.
// Returns a *NotFoundError when no entities are found.
func (zsq *ZoneSerialQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = zsq.Limit(2).IDs(setContextOp(ctx, zsq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{zoneserial.Label}
	default:
		err = &NotSingularError{zoneserial.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (zsq *ZoneSerialQuery) OnlyIDX(ctx context.Context) int {
	id, err := zsq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ZoneSerials.
func (zsq *ZoneSerialQuery) All(ctx context.Context) ([]*ZoneSerial, error) {
	ctx = setContextOp(ctx, zsq.ctx, "All")
	if err := zsq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ZoneSerial, *ZoneSerialQuery]()
	return withInterceptors[[]*ZoneSerial](ctx, zsq, qr, zsq.inters)
}

// AllX is like All, but panics if an error occurs.
func (zsq *ZoneSerialQuery) AllX(ctx context.Context) []*ZoneSerial {
	nodes, err := zsq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ZoneSerial IDs.
func (zsq *ZoneSerialQuery) IDs(ctx context.Context) (ids []int, err error) {
	if zsq.ctx.Unique == nil && zsq.path != nil {
		zsq.Unique(true)
	}
	ctx = setContextOp(ctx, zsq.ctx, "IDs")
	if err = zsq.Select(zoneserial.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (zsq *ZoneSerialQuery) IDsX(ctx context.Context) []int {
	ids, err := zsq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (zsq *ZoneSerialQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, zsq.ctx, "Count")
	if err := zsq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, zsq, querierCount[*ZoneSerialQuery](), zsq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (zsq *ZoneSerialQuery) CountX(ctx context.Context) int {
	count, err := zsq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (zsq *ZoneSerialQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, zsq.ctx, "Exist")
	switch _, err := zsq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (zsq *ZoneSerialQuery) ExistX(ctx context.Context) bool {
	exist, err := zsq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ZoneSerialQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (zsq *ZoneSerialQuery) Clone() *ZoneSerialQuery {
	if zsq == nil {
		return nil
	}
	return &ZoneSerialQuery{
		config:     zsq.config,
		ctx:        zsq.ctx.Clone(),
		order:      append([]zoneserial.OrderOption{}, zsq.order...),
		inters:     append([]Interceptor{}, zsq.inters...),
		predicates: append([]predicate.ZoneSerial{}, zsq.predicates...),
		// clone intermediate query.
		sql:  zsq.sql.Clone(),
		path: zsq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Zone string `json:"zone,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ZoneSerial.Query().
//		GroupBy(zoneserial.FieldZone).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (zsq *ZoneSerialQuery) GroupBy(field string, fields ...string) *ZoneSerialGroupBy {
	zsq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ZoneSerialGroupBy{build: zsq}
	grbuild.flds = &zsq.ctx.Fields
	grbuild.label = zoneserial.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Zone string `json:"zone,omitempty"`
//	}
//
//	client.ZoneSerial.Query().
//		Select(zoneserial.FieldZone).
//		Scan(ctx, &v)
func (zsq *ZoneSerialQuery) Select(fields ...string) *ZoneSerialSelect {
	zsq.ctx.Fields = append(zsq.ctx.Fields, fields...)
	sbuild := &ZoneSerialSelect{ZoneSerialQuery: zsq}
	sbuild.label = zoneserial.Label
	sbuild.flds, sbuild.scan = &zsq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ZoneSerialSelect configured with the given aggregations.
func (zsq *ZoneSerialQuery) Aggregate(fns ...AggregateFunc) *ZoneSerialSelect {
	return zsq.Select().Aggregate(fns...)
}

func (zsq *ZoneSerialQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range zsq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, zsq); err != nil {
				return err
			}
		}
	}
	for _, f := range zsq.ctx.Fields {
		if !zoneserial.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if zsq.path != nil {
		prev, err := zsq.path(ctx)
		if err != nil {
			return err
		}
		zsq.sql = prev
	}
	return nil
}

func (zsq *ZoneSerialQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ZoneSerial, error) {
	var (
		nodes = []*ZoneSerial{}
		_spec = zsq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ZoneSerial).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ZoneSerial{config: zsq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, zsq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (zsq *ZoneSerialQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := zsq.querySpec()
	_spec.Node.Columns = zsq.ctx.Fields
	if len(zsq.ctx.Fields) > 0 {
		_spec.Unique = zsq.ctx.Unique != nil && *zsq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, zsq.driver, _spec)
}

func (zsq *ZoneSerialQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(zoneserial.Table, zoneserial.Columns, sqlgraph.NewFieldSpec(zoneserial.FieldID, field.TypeInt))
	_spec.From = zsq.sql
	if unique := zsq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if zsq.path != nil {
		_spec.Unique = true
	}
	if fields := zsq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, zoneserial.FieldID)
		for i := range fields {
			if fields[i] != zoneserial.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := zsq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := zsq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := zsq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := zsq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (zsq *ZoneSerialQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(zsq.driver.Dialect())
	t1 := builder.Table(zoneserial.Table)
	columns := zsq.ctx.Fields
	if len(columns) == 0 {
		columns = zoneserial.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if zsq.sql != nil {
		selector = zsq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if zsq.ctx.Unique != nil && *zsq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range zsq.predicates {
		p(selector)
	}
	for _, p := range zsq.order {
		p(selector)
	}
	if offset := zsq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := zsq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ZoneSerialGroupBy is the group-by builder for ZoneSerial entities.
type ZoneSerialGroupBy struct {
	selector
	build *ZoneSerialQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (zsgb *ZoneSerialGroupBy) Aggregate(fns ...AggregateFunc) *ZoneSerialGroupBy {
	zsgb.fns = append(zsgb.fns, fns...)
	return zsgb
}

// Scan applies the selector query and scans the result into the given value.
func (zsgb *ZoneSerialGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, zsgb.build.ctx, "GroupBy")
	if err := zsgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ZoneSerialQuery, *ZoneSerialGroupBy](ctx, zsgb.build, zsgb, zsgb.build.inters, v)
}

func (zsgb *ZoneSerialGroupBy) sqlScan(ctx context.Context, root *ZoneSerialQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(zsgb.fns))
	for _, fn := range zsgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*zsgb.flds)+len(zsgb.fns))
		for _, f := range *zsgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*zsgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := zsgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ZoneSerialSelect is the builder for selecting fields of ZoneSerial entities.
type ZoneSerialSelect struct {
	*ZoneSerialQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (zss *ZoneSerialSelect) Aggregate(fns ...AggregateFunc) *ZoneSerialSelect {
	zss.fns = append(zss.fns, fns...)
	return zss
}

// Scan applies the selector query and scans the result into the given value.
func (zss *ZoneSerialSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, zss.ctx, "Select")
	if err := zss.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ZoneSerialQuery, *ZoneSerialSelect](ctx, zss.ZoneSerialQuery, zss, zss.inters, v)
}

func (zss *ZoneSerialSelect) sqlScan(ctx context.Context, root *ZoneSerialQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(zss.fns))
	for _, fn := range zss.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*zss.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := zss.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
