// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"naust/daemon/internal/store/ent/tenant"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// Tenant is the model entity for the Tenant schema.
type Tenant struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the TenantQuery when eager-loading is set.
	Edges        TenantEdges `json:"edges"`
	selectValues sql.SelectValues
}

// TenantEdges holds the relations/edges for other nodes in the graph.
type TenantEdges struct {
	// Users holds the value of the users edge.
	Users []*User `json:"users,omitempty"`
	// Aliases holds the value of the aliases edge.
	Aliases []*Alias `json:"aliases,omitempty"`
	// WebDomains holds the value of the web_domains edge.
	WebDomains []*WebDomain `json:"web_domains,omitempty"`
	// DNSRecords holds the value of the dns_records edge.
	DNSRecords []*DNSRecord `json:"dns_records,omitempty"`
	// DNSZoneProviders holds the value of the dns_zone_providers edge.
	DNSZoneProviders []*DNSZoneProvider `json:"dns_zone_providers,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [5]bool
}

// UsersOrErr returns the Users value or an error if the edge
// was not loaded in eager-loading.
func (e TenantEdges) UsersOrErr() ([]*User, error) {
	if e.loadedTypes[0] {
		return e.Users, nil
	}
	return nil, &NotLoadedError{edge: "users"}
}

// AliasesOrErr returns the Aliases value or an error if the edge
// was not loaded in eager-loading.
func (e TenantEdges) AliasesOrErr() ([]*Alias, error) {
	if e.loadedTypes[1] {
		return e.Aliases, nil
	}
	return nil, &NotLoadedError{edge: "aliases"}
}

// WebDomainsOrErr returns the WebDomains value or an error if the edge
// was not loaded in eager-loading.
func (e TenantEdges) WebDomainsOrErr() ([]*WebDomain, error) {
	if e.loadedTypes[2] {
		return e.WebDomains, nil
	}
	return nil, &NotLoadedError{edge: "web_domains"}
}

// DNSRecordsOrErr returns the DNSRecords value or an error if the edge
// was not loaded in eager-loading.
func (e TenantEdges) DNSRecordsOrErr() ([]*DNSRecord, error) {
	if e.loadedTypes[3] {
		return e.DNSRecords, nil
	}
	return nil, &NotLoadedError{edge: "dns_records"}
}

// DNSZoneProvidersOrErr returns the DNSZoneProviders value or an error if the edge
// was not loaded in eager-loading.
func (e TenantEdges) DNSZoneProvidersOrErr() ([]*DNSZoneProvider, error) {
	if e.loadedTypes[4] {
		return e.DNSZoneProviders, nil
	}
	return nil, &NotLoadedError{edge: "dns_zone_providers"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Tenant) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case tenant.FieldID:
			values[i] = new(sql.NullInt64)
		case tenant.FieldName:
			values[i] = new(sql.NullString)
		case tenant.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Tenant fields.
func (t *Tenant) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case tenant.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			t.ID = int(value.Int64)
		case tenant.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				t.Name = value.String
			}
		case tenant.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				t.CreatedAt = value.Time
			}
		default:
			t.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Tenant.
// This includes values selected through modifiers, order, etc.
func (t *Tenant) Value(name string) (ent.Value, error) {
	return t.selectValues.Get(name)
}

// QueryUsers queries the "users" edge of the Tenant entity.
func (t *Tenant) QueryUsers() *UserQuery {
	return NewTenantClient(t.config).QueryUsers(t)
}

// QueryAliases queries the "aliases" edge of the Tenant entity.
func (t *Tenant) QueryAliases() *AliasQuery {
	return NewTenantClient(t.config).QueryAliases(t)
}

// QueryWebDomains queries the "web_domains" edge of the Tenant entity.
func (t *Tenant) QueryWebDomains() *WebDomainQuery {
	return NewTenantClient(t.config).QueryWebDomains(t)
}

// QueryDNSRecords queries the "dns_records" edge of the Tenant entity.
func (t *Tenant) QueryDNSRecords() *DNSRecordQuery {
	return NewTenantClient(t.config).QueryDNSRecords(t)
}

// QueryDNSZoneProviders queries the "dns_zone_providers" edge of the Tenant entity.
func (t *Tenant) QueryDNSZoneProviders() *DNSZoneProviderQuery {
	return NewTenantClient(t.config).QueryDNSZoneProviders(t)
}

// Update returns a builder for updating this Tenant.
// Note that you need to call Tenant.Unwrap() before calling this method if this Tenant
// was returned from a transaction, and the transaction was committed or rolled back.
func (t *Tenant) Update() *TenantUpdateOne {
	return NewTenantClient(t.config).UpdateOne(t)
}

// Unwrap unwraps the Tenant entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (t *Tenant) Unwrap() *Tenant {
	_tx, ok := t.config.driver.(*txDriver)
	if !ok {
		panic("ent: Tenant is not a transactional entity")
	}
	t.config.driver = _tx.drv
	return t
}

// String implements the fmt.Stringer.
func (t *Tenant) String() string {
	var builder strings.Builder
	builder.WriteString("Tenant(")
	builder.WriteString(fmt.Sprintf("id=%v, ", t.ID))
	builder.WriteString("name=")
	builder.WriteString(t.Name)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(t.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// Tenants is a parsable slice of Tenant.
type Tenants []*Tenant
