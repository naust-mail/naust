# Fail2Ban filter for Naust encryption auth failures.
# Matches failed recovery code and password attempts on the encryption endpoints.

[INCLUDES]

before = common.conf

[Definition]

_daemon = naust

failregex = Naust Management Daemon: Encryption auth failure from ip <HOST> - timestamp .*
ignoreregex =
