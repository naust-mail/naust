[Definition]
# Matches failed Radicale CardDAV/CalDAV login attempts from Radicale's own log.
# Radicale logs failed auth with the client address at the start of the line.
failregex = ^WARNING .*Login attempt.*from.*<HOST>.*failed
            ^.*\[<HOST>\].*unauthorized
ignoreregex =
